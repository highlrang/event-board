spring:
  profiles:
    include: db, db-aws

  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true

  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  mvc:
    static-path-pattern: /static/**

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace